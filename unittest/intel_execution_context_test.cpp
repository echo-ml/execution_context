#include <echo/intel_execution_context.h>
#include <echo/test.h>
using namespace echo;
using namespace echo::execution_context;

TEST_CASE("intel_execution_context") {
  // intel::ExecutionContext execution_context;
  // auto allocator = make_allocator<double>(execution_context);
}
